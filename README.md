# Before It Spoils — client

See the [root README](../README.md) for problem statement, features, stack, setup, and API docs.

```bash
cp .env.example .env   # set VITE_API_URL
npm install
npm run dev
```
